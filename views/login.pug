extends layout

block content
  h1 Login
  form(id="loginForm" action='/login', method='POST')
    input(type='text', name='apiKey', placeholder='Enter API Key')
    input(type='text', name='appID', placeholder='Enter App ID')
    button(type='submit') Login

  if errorMessage
    p.error #{errorMessage}